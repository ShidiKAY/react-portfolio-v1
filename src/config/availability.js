/**
 * Timezone and default availability for Remote-Ready Dashboard.
 * Modify MY_TIMEZONE for your local time; CLIENT_TIMEZONE for Paris (or client HQ).
 */
export const CLIENT_TIMEZONE = "Europe/Paris";
export const MY_TIMEZONE = "Asia/Bangkok";
export const DEFAULT_AVAILABLE = true;
